<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kajian Islam</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"/>
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: #f9fafb;
    }
    .gradient-bg {
      background: linear-gradient(135deg, #059669 0%, #10b981 100%);
    }
    .card-hover {
      transition: all 0.3s ease;
    }
    .card-hover:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1),
                  0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
    .navbar-blur {
      -webkit-backdrop-filter: blur(10px);
      backdrop-filter: blur(10px);
      background: rgba(255, 255, 255, 0.9);
    }
    .mobile-menu {
      transform: translateX(100%);
      transition: transform 0.3s ease-in-out;
    }
    .mobile-menu.active {
      transform: translateX(0);
    }
    .hero-pattern {
      background-image: radial-gradient(circle at 25px 25px, rgba(255,255,255,0.1) 2px, transparent 2px);
      background-size: 50px 50px;
    }
    .glass-card {
      background: rgba(255, 255, 255, 0.95);
      -webkit-backdrop-filter: blur(10px);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    /* Tambahan biar lebih bagus */
    h1, h3 {
      color: #064e3b;
    }
    button {
      cursor: pointer;
    }
    .btn-outline {
      border: 2px solid #059669;
      color: #059669;
      padding: 12px 24px;
      border-radius: 9999px;
      font-weight: 600;
      transition: all 0.3s ease;
    }
    .btn-outline:hover {
      background-color: #059669;
      color: white;
    }
    .upi-loading {
      animation: pulse 1.5s ease-in-out infinite;
    }
    @keyframes pulse {
      0%, 100% { opacity: 0.5; }
      50% { opacity: 1; }
    }
  </style>
</head>
<body>

  <main class="text-black" style="margin-top: 100px;">
    <!-- Hero Section -->
    <section class="mt-10 text-center">
      <h1 class="text-3xl font-bold text-green-700 mb-6">
        Kajian Islam
      </h1>
      <p class="text-gray-700 max-w-3xl mx-auto mb-10 px-6">
        Pengajian rutin yang membahas berbagai tema keislaman dengan pemateri berpengalaman. 
        Ikuti kajian secara online dan offline untuk memperdalam ilmu agama dan meningkatkan keimanan.
      </p>
    </section>

    <!-- Cards Section -->
    <section class="max-w-6xl mx-auto px-6" x-data="{kajianData : discover_kajian()}">
      <div class="upi-loading text-center text-5xl w-full h-full" style="height: 500px;">Loading</div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <template x-for="kajian in (await kajianData).data">
          <article class="bg-white rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 overflow-hidden card-hover">
            <div class="relative">
              <img alt="Ustadz giving a lecture in mosque with attendees listening attentively"
                   class="w-full h-64 object-cover"
                   :src="kajian.thumbnail_url"/>
              <div class="absolute top-4 left-4">
                <span class="bg-green-600 text-white px-3 py-1 rounded-full text-xs font-semibold">Tafsir</span>
              </div>
            </div>
            <div class="p-6">
              <h3 class="font-bold text-xl mb-3 text-gray-800 leading-tight" x-text="kajian.judul"></h3>
              <p class="text-sm text-gray-600 mb-6 leading-relaxed" x-text="kajian.deskripsi"></p>
              
              <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-4 space-y-2 sm:space-y-0">
                <div class="text-xs text-gray-500 flex items-center">
                  <i class="far fa-calendar-alt mr-2 text-green-600"></i>
                  <span x-text="cetak_tenggal(kajian.waktu_mulai)"></span>
                </div>
                <div class="text-xs text-gray-500 flex items-center">
                  <i class="far fa-user mr-2 text-green-600"></i>
                  <span x-text="kajian.pemateri"></span>
                </div>
                <div class="text-xs text-gray-500 flex items-center">
                  <i class="far fa-location-dot mr-2 text-green-600"></i>
                  <span x-text="kajian.lokasi"></span>
                </div>
              </div>

              <button @click="window.location.href = '/kajian/' + kajian.slug"
                      class="bg-green-600 hover:bg-green-700 text-white font-semibold rounded-lg px-6 py-2 text-sm transition-colors duration-200">
                Selengkapnya
              </button>
            </div>
          </article>
        </template>
      </div>

      <div class="text-center mt-12">
        <button @click="window.location.href = '/kelas'" class="btn-outline">
          Lihat Kelas
        </button>
      </div>
    </section>
  </main>

  <!-- Script -->
  <script src="/src/js/kajian.js"></script>
  <script src="https://unpkg.com/alpinejs" defer></script>
</body>
</html>
